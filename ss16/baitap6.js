const vnd = 25000;
let usd = prompt('Nhập số tiền USD cần đổi: ');
let result =  parseInt(usd) * vnd;
alert(`Số tiền sau khi đổi là: ${result} đ`);
document.write(`Số tiền sau khi đổi là: ${result} đ`);